#include<stdio.h>

int main(){
    int x,sum=0;
    printf("Enter any number : ");
    scanf("%d",&x);
    for(int i=1; i<=x; i++){
        if(x%i==0){
            sum = sum + i;
        }
    }
    printf("Sum of all divisors of %d : %d",x,sum);
    return 0;
}
