#include<stdio.h>
#include<math.h>
int main(){
    float x,y;
    int z;
    printf("Enter the number : ");
    scanf("%f",&x);
    y=sqrt(x);
    z=y;
    if(z==y){
        printf("%d",z);
    }
    else
    printf("0");
    return 0;
}
